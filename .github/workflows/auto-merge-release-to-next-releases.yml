name: Auto-merge releas to next releases
on:
  push:
    branches:
      - 'release-[0-9]+.[0-9]+.[0-9]+'
jobs:
  auto-merge-rc-job:
    runs-on: ubuntu-20.04
    steps:
      - name: Check out repository code
        uses: actions/checkout@v1
      - name: Auto-merge
        uses: sergeevichvn/gh-action-nightly-merge@v1.3.5
        env:
          GITHUB_TOKEN: ${{ secrets.GIT_ACCESS_TOKEN }}
      - name: Send message to Telegram chat
        id: sendMessageToTelegram
        if: failure()
        uses: dawidd6/action-send-mail@v3.6.0
        with:
          server_address: ${{secrets.SERVER_ADDRESS}} #smtp.gmail.com
          server_port: ${{secrets.SERVER_PORT}} #465
          username: ${{secrets.FROM_MAIL_USERNAME}}
          password: ${{secrets.MAIL_PASSWORD}} #5$FrN}52b0TA
          subject: Github Actions job result
          to: ${{secrets.TO_MAIL_USERNAME}} #semenovio@sovcombank.ru
          from: auto-merge-rc-job
          body: |
            🔥 [AUTO-MERGE FAIL](https://github.com/${{github.repository}}/actions/runs/${{github.run_id}})
            Repository: [${{ github.repository }}](https://github.com/${{ github.repository }})
            Author of PR: [${{ github.actor }}](https://github.com/${{ github.actor }})
            TG CHAT
        continue-on-error: true
      - name: Check on failure step "sendMessageToTelegram"
        if: ${{ failure() }} && ${{ steps.sendMessageToTelegram.outcome }} == failure
        uses: dawidd6/action-send-mail@v3.6.0
        with:
          server_address: ${{secrets.SERVER_ADDRESS}} #smtp.gmail.com
          server_port: ${{secrets.SERVER_PORT}} #465
          username: ${{secrets.FROM_MAIL_USERNAME}}
          password: ${{secrets.MAIL_PASSWORD}}
          subject: Github Actions job result
          to: ${{secrets.TO_MAIL_USERNAME}} #semenovio@sovcombank.ru
          from: auto-merge-rc-job
          body: |
            🔥 [AUTO-MERGE FAIL](https://github.com/${{github.repository}}/actions/runs/${{github.run_id}})
            Repository: [${{ github.repository }}](https://github.com/${{ github.repository }})
            Author of PR: [${{ github.actor }}](https://github.com/${{ github.actor }})
            Error sending message to Telegram chat
